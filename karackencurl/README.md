# karackencurl

Vendored copy of the `karackencurllib` page-curl renderer from
https://github.com/Darkaxt/PlayLikeCurl (OpenGL ES 2 curl animation).

Only `src/main` sources are included; the Gradle module metadata is local to this project.
